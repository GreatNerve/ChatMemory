/** Summarize when local history exceeds this many messages (user + assistant). */
export const SUMMARIZE_THRESHOLD = 24;

/** Keep this many recent turns verbatim after summarization. */
export const KEEP_RECENT = 10;
